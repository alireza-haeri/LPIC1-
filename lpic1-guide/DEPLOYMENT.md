# راهنمای استقرار / Deployment Guide

این سند راهنمای کامل استقرار وب‌سایت مستندات LPIC-1 را ارائه می‌دهد.

## 🚀 استقرار خودکار با GitHub Actions

پس از merge شدن این PR به شاخه `main`، وب‌سایت به طور خودکار به GitHub Pages منتشر خواهد شد.

### فعال‌سازی GitHub Pages

1. به تنظیمات مخزن بروید: `Settings` → `Pages`
2. در بخش **Source**:
   - Branch: `gh-pages`
   - Folder: `/ (root)`
3. روی **Save** کلیک کنید

### URL وب‌سایت

پس از استقرار، وب‌سایت در آدرس زیر در دسترس خواهد بود:

```
https://alireza-haeri.github.io/LPIC1-/
```

## 🔄 فرآیند استقرار خودکار

هر بار که تغییرات در دایرکتوری `lpic1-guide/` به شاخه `main` push می‌شود:

1. GitHub Actions workflow اجرا می‌شود
2. محیط Python راه‌اندازی می‌شود
3. وابستگی‌ها نصب می‌شوند
4. وب‌سایت با `mkdocs build` ساخته می‌شود
5. فایل‌های ساخته شده به شاخه `gh-pages` push می‌شوند
6. GitHub Pages وب‌سایت را منتشر می‌کند

## 🛠️ استقرار دستی

برای استقرار دستی:

```bash
cd lpic1-guide
mkdocs gh-deploy --force
```

این دستور:
- وب‌سایت را می‌سازد
- به شاخه `gh-pages` push می‌کند
- به طور خودکار GitHub Pages را به‌روز می‌کند

## 🔍 بررسی وضعیت استقرار

### در GitHub

1. به تب **Actions** مخزن بروید
2. آخرین workflow را ببینید
3. اگر موفقیت‌آمیز بود، یک تیک سبز نمایش داده می‌شود

### لاگ‌های استقرار

```bash
# مشاهده لاگ‌های GitHub Actions در صفحه Actions
# یا اجرای دستور زیر برای بررسی محلی:
cd lpic1-guide
mkdocs build --verbose
```

## 🌐 دامنه سفارشی (اختیاری)

برای استفاده از دامنه سفارشی:

1. فایل `CNAME` در دایرکتوری `docs/` ایجاد کنید:
   ```
   docs.your-domain.com
   ```

2. رکورد DNS خود را پیکربندی کنید:
   ```
   Type: CNAME
   Name: docs
   Value: alireza-haeri.github.io
   ```

3. در تنظیمات GitHub Pages، دامنه سفارشی را وارد کنید

## 🔒 HTTPS

GitHub Pages به طور خودکار HTTPS را فعال می‌کند. اطمینان حاصل کنید که:

- ✅ **Enforce HTTPS** در تنظیمات فعال است
- ✅ گواهی SSL صادر شده است (ممکن است چند دقیقه طول بکشد)

## 📱 سایر پلتفرم‌های استقرار

### Netlify

1. مخزن را به Netlify متصل کنید
2. تنظیمات Build:
   ```
   Build command: cd lpic1-guide && mkdocs build
   Publish directory: lpic1-guide/site
   ```

### Vercel

1. مخزن را import کنید
2. تنظیمات:
   ```
   Framework Preset: Other
   Build Command: cd lpic1-guide && pip install -r requirements.txt && mkdocs build
   Output Directory: lpic1-guide/site
   ```

### سرور شخصی

```bash
# ساخت وب‌سایت
cd lpic1-guide
mkdocs build

# آپلود فایل‌های site/ به سرور
rsync -avz site/ user@server:/var/www/html/lpic1/
```

## 🐛 عیب‌یابی

### وب‌سایت نمایش داده نمی‌شود

1. بررسی کنید که GitHub Pages فعال است
2. شاخه `gh-pages` وجود دارد
3. workflow با موفقیت اجرا شده است
4. چند دقیقه برای انتشار صبر کنید

### خطای 404

1. مسیر `site_url` در `mkdocs.yml` را بررسی کنید
2. اطمینان حاصل کنید که `/LPIC1-/` در مسیرهای URL وجود دارد

### تغییرات اعمال نمی‌شود

1. کش مرورگر را پاک کنید (Ctrl+Shift+R)
2. بررسی کنید که commit به `main` push شده است
3. وضعیت workflow را در Actions بررسی کنید

## 📊 آمار وب‌سایت

پس از استقرار، می‌توانید:

- Google Analytics اضافه کنید
- GitHub Insights را بررسی کنید
- تعداد بازدیدکنندگان را پیگیری کنید

## 🔄 به‌روزرسانی محتوا

برای اضافه کردن صفحات جدید:

1. فایل `.md` جدید در `docs/` ایجاد کنید
2. آن را به `nav` در `mkdocs.yml` اضافه کنید
3. commit و push کنید
4. وب‌سایت خودکار به‌روز می‌شود

## ✅ چک‌لیست استقرار

- [ ] GitHub Pages فعال شده
- [ ] شاخه `gh-pages` موجود است
- [ ] Workflow موفقیت‌آمیز اجرا شده
- [ ] وب‌سایت در دسترس است
- [ ] RTL به درستی کار می‌کند
- [ ] جستجو فعال است
- [ ] تمام لینک‌ها کار می‌کنند
- [ ] تصاویر بارگذاری می‌شوند
- [ ] طراحی واکنش‌گرا است

## 📞 پشتیبانی

اگر مشکلی در استقرار داشتید:

1. لاگ‌های GitHub Actions را بررسی کنید
2. مستندات MkDocs را مطالعه کنید: https://www.mkdocs.org/
3. یک Issue در مخزن ایجاد کنید

---

**نکته**: این راهنما مخصوص استقرار در GitHub Pages است. برای سایر پلتفرم‌ها، به مستندات آنها مراجعه کنید.
